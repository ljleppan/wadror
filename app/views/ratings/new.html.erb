<h1>Create new rating</h1>

<%= form_for(@rating) do |f| %>

    <% if @rating.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@rating.errors.count, "error") %> prohibited the rating from being saved:</h2>
          </h2>

          <ul>
            <% @rating.errors.full_messages.each do |msg| %>
            <li>
              <%= msg %>
            </li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.label :beer_id %> <br>
    <%= f.select :beer_id, options_from_collection_for_select(@beers, :id, :to_s) %> <br>

    <%= f.label :score %> <br>
    <%= f.number_field :score, min:0 %>

    <%= f.submit %>

    <span class="tools">
      <%= link_to 'Back', ratings_path %>
    </span>

<% end %>