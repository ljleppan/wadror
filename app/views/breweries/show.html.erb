<h1><%= @brewery.name %></h1>

<p id="notice"><%= notice %></p>

<p>
  <i>Established at <%= @brewery.year %></i>
</p>

<p>
  Number of beers
  <%= @brewery.beers.count %>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer %>
  <% end %>
</p>

<p>
  <% if @brewery.ratings.empty? %>
      The brewery has no ratings!
  <% else %>
      Has <%=pluralize(@brewery.ratings.count, 'rating')%>, average rating <%= @brewery.average_rating %>
  <% end %>
</p>

<% if current_user %>
  <%= link_to 'Edit', edit_brewery_path(@brewery) %> |
  <%= link_to 'Destroy', @beer_club, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>
<%= link_to 'Back', breweries_path %>
